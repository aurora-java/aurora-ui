<div style="${size};overflow:auto;" class="${wrapClass}" id="${id}"><div atype="tree.body"></div></div>
<script>
	new Aurora.Tree(${config});
	$$('${id}').bind('${dataset}');
	${binding}
	${events}
</script>