<table id="${id}" class="${wrapClass}" style="width:${width}px;height:${height}px;${style}" cellpadding="0" cellspacing="0" border="0">
	${rooticon}
	<tbody>
		<tr>
			${rootline}
			<td class="menu-tree-body">
			</td>
		</tr>
	</tbody>
</table>
<script>
	new Aurora.MenuTree(${config});
	$('${id}').bind('${bindtarget}');
</script>