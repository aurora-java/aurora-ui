<?xml version="1.0" encoding="UTF-8"?>
<sql-service trace="true">
	<parameters>
		<parameter name="EMPNO"  dataType="java.lang.Long" input="false" output="true" inputPath="@EMPNO" outputPath="@EMPNO"/>
	</parameters>
	<update>
	    begin
			insert into EMP(EMPNO, ENAME, HIREDATE,JOB, MGR, SAL,DEPTNO,CREATION_DATE) values(
				${/parameter/@EMPNO},
				${@ename},
				to_date(${@hiredate},'yyyy-mm-dd'),
				${@job},
				${@mgr},
				${@sal},
				${@deptno},
				sysdate
			) returning EMPNO into ${@EMPNO};
		end;
	</update>
</sql-service>